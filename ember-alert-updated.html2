<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>Ember Alert Services LLC — Concrete Experts | Wake County, NC</title>
<link rel="preconnect" href="https://fonts.googleapis.com"/>
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin/>
<link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Barlow:ital,wght@0,300;0,400;0,600;0,700;1,300&family=Barlow+Condensed:wght@400;600;700&display=swap" rel="stylesheet"/>
<style>
  :root {
    --black: #0a0a0a;
    --deep: #111111;
    --surface: #1a1a1a;
    --raised: #222222;
    --border: #2e2e2e;
    --white: #f0ede8;
    --white-dim: rgba(240,237,232,0.6);
    --white-faint: rgba(240,237,232,0.08);
    --ember: #c0392b;
    --ember-bright: #e74c3c;
    --ember-glow: rgba(192,57,43,0.15);
    --concrete: #5a5a5a;
    --concrete-light: #8a8a8a;
    --gold: #c9a84c;
    --gold-dim: rgba(201,168,76,0.2);
  }

  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  html { scroll-behavior: smooth; }

  body {
    background: var(--black);
    color: var(--white);
    font-family: 'Barlow', sans-serif;
    font-weight: 300;
    overflow-x: hidden;
    cursor: none;
  }

  /* CUSTOM CURSOR */
  #cursor {
    position: fixed;
    width: 12px;
    height: 12px;
    background: var(--ember);
    border-radius: 50%;
    pointer-events: none;
    z-index: 9999;
    transform: translate(-50%, -50%);
    transition: transform 0.1s, width 0.2s, height 0.2s, background 0.2s;
    mix-blend-mode: normal;
  }
  #cursor-ring {
    position: fixed;
    width: 36px;
    height: 36px;
    border: 1px solid rgba(192,57,43,0.5);
    border-radius: 50%;
    pointer-events: none;
    z-index: 9998;
    transform: translate(-50%, -50%);
    transition: transform 0.08s, width 0.3s, height 0.3s, border-color 0.3s;
  }
  body:has(a:hover) #cursor, body:has(button:hover) #cursor { width: 20px; height: 20px; background: var(--ember-bright); }
  body:has(a:hover) #cursor-ring, body:has(button:hover) #cursor-ring { width: 52px; height: 52px; border-color: var(--ember); }

  /* NOISE TEXTURE OVERLAY */
  body::before {
    content: '';
    position: fixed;
    inset: 0;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.04'/%3E%3C/svg%3E");
    pointer-events: none;
    z-index: 1000;
    opacity: 0.4;
  }

  /* NAV */
  nav {
    position: fixed;
    top: 0; left: 0; right: 0;
    z-index: 500;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0.6rem 4rem;
    background: rgba(10,10,10,0.85);
    backdrop-filter: blur(12px);
    border-bottom: 1px solid var(--border);
    transition: padding 0.3s, background 0.3s;
  }
  nav:hover, nav.scrolled { padding: 1.2rem 4rem; background: rgba(10,10,10,0.97); }

  .nav-logo {
    display: flex;
    align-items: center;
    gap: 0.8rem;
    text-decoration: none;
  }

  /* SVG CREST LOGO */
  .crest-svg { width: 52px; height: 52px; flex-shrink: 0; }

  .logo-text-group { display: flex; flex-direction: column; line-height: 1; }
  .logo-name {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 1.3rem;
    letter-spacing: 0.12em;
    color: var(--white);
  }
  .logo-sub {
    font-family: 'Barlow Condensed', sans-serif;
    font-size: 0.6rem;
    letter-spacing: 0.3em;
    color: var(--concrete-light);
    text-transform: uppercase;
  }

  .nav-links {
    display: flex;
    gap: 2.5rem;
    list-style: none;
  }
  .nav-links a {
    font-family: 'Barlow Condensed', sans-serif;
    font-size: 0.75rem;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: var(--white-dim);
    text-decoration: none;
    transition: color 0.2s;
    position: relative;
  }
  .nav-links a::after {
    content: '';
    position: absolute;
    bottom: -4px; left: 0;
    width: 0; height: 1px;
    background: var(--ember);
    transition: width 0.3s;
  }
  .nav-links a:hover { color: var(--white); }
  .nav-links a:hover::after { width: 100%; }

  .nav-cta {
    font-family: 'Barlow Condensed', sans-serif;
    font-size: 0.72rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    background: var(--ember);
    color: var(--white);
    border: none;
    padding: 0.65rem 1.5rem;
    cursor: none;
    transition: background 0.2s, transform 0.2s;
  }
  .nav-cta:hover { background: var(--ember-bright); transform: translateY(-1px); }

  /* HERO */
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: flex-end;
    padding: 100px 4rem 6rem;
    position: relative;
    overflow: hidden;
  }

  /* Concrete texture background */
  .hero-bg {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(160deg, rgba(10,10,10,0) 30%, rgba(192,57,43,0.04) 100%),
      repeating-linear-gradient(
        90deg,
        transparent,
        transparent 2px,
        rgba(255,255,255,0.005) 2px,
        rgba(255,255,255,0.005) 4px
      ),
      repeating-linear-gradient(
        0deg,
        transparent,
        transparent 2px,
        rgba(255,255,255,0.005) 2px,
        rgba(255,255,255,0.005) 4px
      );
  }

  /* Animated ember particles */
  .particles { position: absolute; inset: 0; pointer-events: none; }
  .particle {
    position: absolute;
    width: 2px;
    height: 2px;
    background: var(--ember);
    border-radius: 50%;
    animation: float-up linear infinite;
    opacity: 0;
  }
  @keyframes float-up {
    0% { transform: translateY(0) translateX(0); opacity: 0; }
    10% { opacity: 0.8; }
    90% { opacity: 0.2; }
    100% { transform: translateY(-80vh) translateX(var(--drift)); opacity: 0; }
  }

  /* Large background number */
  .hero-bg-number {
    position: absolute;
    right: -2rem;
    bottom: -3rem;
    font-family: 'Bebas Neue', sans-serif;
    font-size: 28vw;
    color: rgba(255,255,255,0.018);
    line-height: 1;
    pointer-events: none;
    user-select: none;
    letter-spacing: -0.02em;
  }

  .hero-content { position: relative; z-index: 2; max-width: 860px; }

  .hero-eyebrow {
    font-family: 'Barlow Condensed', sans-serif;
    font-size: 0.72rem;
    letter-spacing: 0.4em;
    text-transform: uppercase;
    color: var(--ember);
    margin-bottom: 1.5rem;
    display: flex;
    align-items: center;
    gap: 1rem;
  }
  .hero-eyebrow::before {
    content: '';
    display: block;
    width: 40px;
    height: 1px;
    background: var(--ember);
  }

  .hero-title {
    font-family: 'Bebas Neue', sans-serif;
    font-size: clamp(4.5rem, 9vw, 9rem);
    line-height: 0.92;
    letter-spacing: 0.02em;
    margin-bottom: 2rem;
  }
  .hero-title .line-accent { color: var(--ember); }
  .hero-title .line-dim { color: var(--white-dim); }

  .hero-subtitle {
    font-size: 1rem;
    line-height: 1.8;
    color: var(--white-dim);
    max-width: 480px;
    margin-bottom: 3rem;
    font-weight: 300;
    letter-spacing: 0.02em;
  }

  .hero-actions { display: flex; align-items: center; gap: 2rem; }

  .btn-primary {
    font-family: 'Barlow Condensed', sans-serif;
    font-size: 0.8rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    background: var(--ember);
    color: var(--white);
    border: none;
    padding: 1.1rem 2.5rem;
    cursor: none;
    transition: background 0.2s, transform 0.2s, box-shadow 0.2s;
    text-decoration: none;
    display: inline-block;
    position: relative;
    overflow: hidden;
  }
  .btn-primary::before {
    content: '';
    position: absolute;
    top: 0; left: -100%;
    width: 100%; height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.1), transparent);
    transition: left 0.4s;
  }
  .btn-primary:hover { background: var(--ember-bright); transform: translateY(-2px); box-shadow: 0 8px 30px rgba(192,57,43,0.4); }
  .btn-primary:hover::before { left: 100%; }

  .btn-ghost {
    font-family: 'Barlow Condensed', sans-serif;
    font-size: 0.8rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    background: transparent;
    color: var(--white-dim);
    border: 1px solid var(--border);
    padding: 1.1rem 2.5rem;
    cursor: none;
    transition: color 0.2s, border-color 0.2s;
    text-decoration: none;
    display: inline-block;
  }
  .btn-ghost:hover { color: var(--white); border-color: var(--concrete-light); }

  .hero-stats {
    position: absolute;
    right: 4rem;
    bottom: 6rem;
    display: flex;
    flex-direction: column;
    gap: 2rem;
    z-index: 2;
    text-align: right;
  }
  .hero-stat-num {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 2.8rem;
    color: var(--white);
    line-height: 1;
  }
  .hero-stat-num span { color: var(--ember); }
  .hero-stat-label {
    font-family: 'Barlow Condensed', sans-serif;
    font-size: 0.62rem;
    letter-spacing: 0.3em;
    text-transform: uppercase;
    color: var(--concrete-light);
  }

  /* Scroll indicator */
  .scroll-hint {
    position: absolute;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    z-index: 2;
  }
  .scroll-hint span {
    font-family: 'Barlow Condensed', sans-serif;
    font-size: 0.6rem;
    letter-spacing: 0.3em;
    text-transform: uppercase;
    color: var(--concrete-light);
  }
  .scroll-line {
    width: 1px;
    height: 40px;
    background: linear-gradient(to bottom, var(--ember), transparent);
    animation: scroll-pulse 2s ease-in-out infinite;
  }
  @keyframes scroll-pulse { 0%,100% { opacity: 0.3; transform: scaleY(1); } 50% { opacity: 1; transform: scaleY(1.2); } }

  /* SECTION SHARED */
  section { padding: 7rem 4rem; position: relative; }
  .section-label {
    font-family: 'Barlow Condensed', sans-serif;
    font-size: 0.65rem;
    letter-spacing: 0.45em;
    text-transform: uppercase;
    color: var(--ember);
    margin-bottom: 1rem;
    display: flex;
    align-items: center;
    gap: 1rem;
  }
  .section-label::before { content: ''; display: block; width: 30px; height: 1px; background: var(--ember); }

  .section-title {
    font-family: 'Bebas Neue', sans-serif;
    font-size: clamp(2.8rem, 5vw, 4.5rem);
    line-height: 0.95;
    letter-spacing: 0.03em;
    margin-bottom: 1.5rem;
  }

  /* SERVICES */
  .services { background: var(--deep); }
  .services-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1px;
    margin-top: 4rem;
    background: var(--border);
  }
  .service-card {
    background: var(--deep);
    padding: 2.5rem 2rem;
    position: relative;
    overflow: hidden;
    cursor: none;
    transition: background 0.3s;
  }
  .service-card::before {
    content: '';
    position: absolute;
    bottom: 0; left: 0;
    width: 0; height: 2px;
    background: var(--ember);
    transition: width 0.4s cubic-bezier(0.4,0,0.2,1);
  }
  .service-card:hover { background: var(--surface); }
  .service-card:hover::before { width: 100%; }

  .service-icon {
    width: 48px;
    height: 48px;
    margin-bottom: 1.5rem;
    color: var(--ember);
  }
  .service-num {
    position: absolute;
    top: 1.5rem; right: 1.5rem;
    font-family: 'Bebas Neue', sans-serif;
    font-size: 1.1rem;
    color: var(--border);
    transition: color 0.3s;
  }
  .service-card:hover .service-num { color: var(--ember); }
  .service-name {
    font-family: 'Barlow Condensed', sans-serif;
    font-size: 1.2rem;
    font-weight: 600;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    margin-bottom: 0.8rem;
    color: var(--white);
  }
  .service-desc {
    font-size: 0.85rem;
    line-height: 1.7;
    color: var(--white-dim);
  }

  /* STATS COUNTER SECTION */
  .stats-section {
    background: var(--ember);
    padding: 5rem 4rem;
  }
  .stats-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 2rem;
  }
  .stat-item { text-align: center; }
  .stat-number {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 4.5rem;
    line-height: 1;
    color: var(--white);
    display: block;
  }
  .stat-label {
    font-family: 'Barlow Condensed', sans-serif;
    font-size: 0.7rem;
    letter-spacing: 0.3em;
    text-transform: uppercase;
    color: rgba(240,237,232,0.7);
    margin-top: 0.5rem;
    display: block;
  }

  /* BEFORE / AFTER INTERACTIVE */
  .before-after-section { background: var(--black); }
  .ba-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-end;
    margin-bottom: 3rem;
  }
  .ba-intro { max-width: 400px; font-size: 0.9rem; line-height: 1.8; color: var(--white-dim); }

  .ba-slider-wrap {
    position: relative;
    width: 100%;
    max-width: 900px;
    margin: 0 auto;
    height: 480px;
    overflow: hidden;
    cursor: none;
    border: 1px solid var(--border);
  }

  .ba-before, .ba-after {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: 'Bebas Neue', sans-serif;
    font-size: 1.5rem;
    letter-spacing: 0.1em;
  }

  /* Simulated concrete textures */
  .ba-before {
    background:
      repeating-linear-gradient(45deg, #1a1a1a, #1a1a1a 2px, #151515 2px, #151515 8px),
      repeating-linear-gradient(-45deg, transparent, transparent 3px, rgba(0,0,0,0.2) 3px, rgba(0,0,0,0.2) 6px);
    background-color: #2a2520;
  }
  .ba-after {
    background:
      repeating-linear-gradient(90deg, #1a1918, #1a1918 3px, #1c1b1a 3px, #1c1b1a 40px),
      repeating-linear-gradient(0deg, #1a1918, #1a1918 3px, #1c1b1a 3px, #1c1b1a 40px);
    background-color: #1a1918;
  }

  .ba-before-label, .ba-after-label {
    position: absolute;
    bottom: 1.5rem;
    font-family: 'Barlow Condensed', sans-serif;
    font-size: 0.65rem;
    letter-spacing: 0.35em;
    text-transform: uppercase;
    padding: 0.4rem 0.8rem;
    background: rgba(0,0,0,0.6);
    backdrop-filter: blur(4px);
  }
  .ba-before-label { left: 1.5rem; color: var(--concrete-light); border: 1px solid var(--border); }
  .ba-after-label { right: 1.5rem; color: var(--white); border: 1px solid var(--ember); }

  /* Fake "before" cracked surface */
  .ba-before-content {
    position: absolute;
    inset: 0;
    overflow: hidden;
  }
  .crack {
    position: absolute;
    background: rgba(0,0,0,0.6);
    transform-origin: top left;
  }

  /* Fake "after" clean surface with sheen lines */
  .ba-after-content {
    position: absolute;
    inset: 0;
    overflow: hidden;
  }
  .sheen {
    position: absolute;
    top: 0; bottom: 0;
    width: 1px;
    background: rgba(255,255,255,0.04);
  }
  .expansion-joint {
    position: absolute;
    left: 0; right: 0;
    height: 2px;
    background: rgba(0,0,0,0.4);
  }

  .ba-divider {
    position: absolute;
    top: 0; bottom: 0;
    width: 2px;
    background: var(--white);
    z-index: 10;
    cursor: none;
  }
  .ba-handle {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 44px;
    height: 44px;
    background: var(--white);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 0 0 3px var(--ember);
  }
  .ba-handle svg { color: var(--black); width: 18px; height: 18px; }

  .ba-clip {
    position: absolute;
    inset: 0;
    overflow: hidden;
  }

  /* PROCESS SECTION */
  .process-section { background: var(--deep); }
  .process-steps {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: 0;
    margin-top: 4rem;
    position: relative;
  }
  .process-steps::before {
    content: '';
    position: absolute;
    top: 28px;
    left: 10%;
    right: 10%;
    height: 1px;
    background: linear-gradient(to right, transparent, var(--border), var(--border), transparent);
  }
  .process-step { padding: 0 1.5rem; text-align: center; }
  .step-dot {
    width: 56px;
    height: 56px;
    border: 1px solid var(--border);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 1.5rem;
    background: var(--deep);
    position: relative;
    z-index: 1;
    transition: border-color 0.3s, background 0.3s;
    cursor: none;
  }
  .step-dot:hover { border-color: var(--ember); background: var(--ember-glow); }
  .step-dot-num {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 1.3rem;
    color: var(--concrete-light);
    transition: color 0.3s;
  }
  .step-dot:hover .step-dot-num { color: var(--ember); }
  .step-title {
    font-family: 'Barlow Condensed', sans-serif;
    font-size: 0.85rem;
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--white);
    margin-bottom: 0.6rem;
  }
  .step-desc { font-size: 0.78rem; color: var(--white-dim); line-height: 1.6; }

  /* TESTIMONIALS */
  .testimonials-section { background: var(--black); overflow: hidden; }
  .testimonials-track-wrap { margin-top: 3rem; overflow: hidden; position: relative; }
  .testimonials-track-wrap::before,
  .testimonials-track-wrap::after {
    content: '';
    position: absolute;
    top: 0; bottom: 0;
    width: 120px;
    z-index: 2;
    pointer-events: none;
  }
  .testimonials-track-wrap::before { left: 0; background: linear-gradient(to right, var(--black), transparent); }
  .testimonials-track-wrap::after { right: 0; background: linear-gradient(to left, var(--black), transparent); }

  .testimonials-track {
    display: flex;
    gap: 1.5rem;
    animation: marquee 30s linear infinite;
    width: max-content;
  }
  .testimonials-track:hover { animation-play-state: paused; }
  @keyframes marquee { from { transform: translateX(0); } to { transform: translateX(-50%); } }

  .testimonial-card {
    width: 340px;
    flex-shrink: 0;
    background: var(--surface);
    border: 1px solid var(--border);
    padding: 2rem;
    position: relative;
    transition: border-color 0.3s;
    cursor: none;
  }
  .testimonial-card:hover { border-color: var(--ember); }
  .testimonial-card::before {
    content: '"';
    position: absolute;
    top: -0.5rem; left: 1.5rem;
    font-family: 'Bebas Neue', sans-serif;
    font-size: 5rem;
    color: var(--ember);
    opacity: 0.3;
    line-height: 1;
  }
  .testimonial-stars {
    display: flex;
    gap: 3px;
    margin-bottom: 1rem;
  }
  .star { color: var(--gold); font-size: 0.8rem; }
  .testimonial-text {
    font-size: 0.85rem;
    line-height: 1.75;
    color: var(--white-dim);
    margin-bottom: 1.5rem;
    font-style: italic;
  }
  .testimonial-author {
    display: flex;
    align-items: center;
    gap: 0.8rem;
  }
  .author-avatar {
    width: 36px;
    height: 36px;
    border-radius: 50%;
    background: var(--raised);
    border: 1px solid var(--border);
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: 'Bebas Neue', sans-serif;
    font-size: 1rem;
    color: var(--ember);
  }
  .author-name {
    font-family: 'Barlow Condensed', sans-serif;
    font-size: 0.8rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--white);
  }
  .author-location { font-size: 0.7rem; color: var(--concrete-light); }

  .testimonial-service-tag {
    position: absolute;
    top: 1.2rem; right: 1.2rem;
    font-family: 'Barlow Condensed', sans-serif;
    font-size: 0.55rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--ember);
    border: 1px solid var(--ember-glow);
    padding: 0.2rem 0.5rem;
    background: var(--ember-glow);
  }

  /* CTA / QUOTE SECTION */
  .quote-section {
    background: var(--surface);
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
  }
  .quote-inner {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 6rem;
    align-items: center;
  }
  .quote-left .section-title { margin-bottom: 1rem; }
  .quote-copy { font-size: 0.9rem; line-height: 1.8; color: var(--white-dim); margin-bottom: 2rem; }
  .quote-trust {
    display: flex;
    gap: 2rem;
    margin-top: 2.5rem;
  }
  .trust-item {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-family: 'Barlow Condensed', sans-serif;
    font-size: 0.7rem;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--concrete-light);
  }
  .trust-dot { width: 6px; height: 6px; background: var(--ember); border-radius: 50%; flex-shrink: 0; }

  /* QUOTE FORM */
  .quote-form { display: flex; flex-direction: column; gap: 1rem; }
  .form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 1rem; }
  .form-group { display: flex; flex-direction: column; gap: 0.4rem; }
  .form-label {
    font-family: 'Barlow Condensed', sans-serif;
    font-size: 0.62rem;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: var(--concrete-light);
  }
  .form-input, .form-select, .form-textarea {
    background: var(--raised);
    border: 1px solid var(--border);
    color: var(--white);
    padding: 0.8rem 1rem;
    font-family: 'Barlow', sans-serif;
    font-size: 0.85rem;
    font-weight: 300;
    outline: none;
    transition: border-color 0.2s, background 0.2s;
    cursor: none;
    -webkit-appearance: none;
  }
  .form-input:focus, .form-select:focus, .form-textarea:focus {
    border-color: var(--ember);
    background: var(--surface);
  }
  .form-select option { background: var(--raised); }
  .form-textarea { resize: vertical; min-height: 90px; }
  .form-submit {
    font-family: 'Barlow Condensed', sans-serif;
    font-size: 0.8rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    background: var(--ember);
    color: var(--white);
    border: none;
    padding: 1.1rem;
    cursor: none;
    transition: background 0.2s, transform 0.2s;
    margin-top: 0.5rem;
    position: relative;
    overflow: hidden;
  }
  .form-submit:hover { background: var(--ember-bright); transform: translateY(-1px); }
  .form-submit.submitted { background: #27ae60; }

  /* FOOTER */
  footer {
    background: var(--black);
    border-top: 1px solid var(--border);
    padding: 3rem 4rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }
  .footer-copy {
    font-family: 'Barlow Condensed', sans-serif;
    font-size: 0.65rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--concrete-light);
  }
  .footer-tagline {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 0.95rem;
    letter-spacing: 0.15em;
    color: var(--border);
  }

  /* REVEAL ANIMATIONS */
  .reveal {
    opacity: 0;
    transform: translateY(30px);
    transition: opacity 0.7s cubic-bezier(0.4,0,0.2,1), transform 0.7s cubic-bezier(0.4,0,0.2,1);
  }
  .reveal.visible { opacity: 1; transform: translateY(0); }
  .reveal-delay-1 { transition-delay: 0.1s; }
  .reveal-delay-2 { transition-delay: 0.2s; }
  .reveal-delay-3 { transition-delay: 0.3s; }
  .reveal-delay-4 { transition-delay: 0.4s; }
</style>
</head>
<body>

<div id="cursor"></div>
<div id="cursor-ring"></div>

<!-- NAV -->
<nav id="main-nav">
  <a href="#" class="nav-logo">
    <!-- CREST LOGO -->
    <svg class="crest-svg" viewBox="0 0 100 110" fill="none" xmlns="http://www.w3.org/2000/svg">
      <!-- Crest shape -->
      <path d="M50 4 L92 20 L92 58 Q92 86 50 106 Q8 86 8 58 L8 20 Z" fill="#1a1a1a" stroke="#c0392b" stroke-width="1.5"/>
      <!-- Inner crest border -->
      <path d="M50 10 L86 24 L86 58 Q86 82 50 100 Q14 82 14 58 L14 24 Z" fill="none" stroke="#2e2e2e" stroke-width="0.8"/>
      <!-- Top banner -->
      <rect x="22" y="16" width="56" height="14" rx="1" fill="#c0392b" opacity="0.9"/>
      <text x="50" y="26.5" text-anchor="middle" fill="#f0ede8" font-family="Bebas Neue, sans-serif" font-size="8" letter-spacing="1.5">EMBER ALERT</text>
      <!-- Cement truck simplified -->
      <!-- Truck body -->
      <rect x="28" y="52" width="26" height="14" rx="1" fill="#c0392b" opacity="0.8"/>
      <!-- Drum -->
      <ellipse cx="46" cy="47" rx="10" ry="7" fill="none" stroke="#c0392b" stroke-width="1.5"/>
      <line x1="40" y1="43" x2="52" y2="51" stroke="#c0392b" stroke-width="1"/>
      <line x1="52" y1="43" x2="40" y2="51" stroke="#c0392b" stroke-width="1"/>
      <!-- Wheels -->
      <circle cx="34" cy="67" r="4" fill="#111" stroke="#5a5a5a" stroke-width="1"/>
      <circle cx="46" cy="67" r="4" fill="#111" stroke="#5a5a5a" stroke-width="1"/>
      <!-- Cab -->
      <rect x="54" y="55" width="14" height="11" rx="1" fill="#8a8a8a" opacity="0.6"/>
      <circle cx="62" cy="67" r="4" fill="#111" stroke="#5a5a5a" stroke-width="1"/>
      <!-- Chute -->
      <line x1="28" y1="60" x2="18" y2="70" stroke="#c0392b" stroke-width="1.5"/>
      <!-- Bottom text -->
      <rect x="22" y="76" width="56" height="12" rx="1" fill="#1a1a1a" stroke="#2e2e2e" stroke-width="0.8"/>
      <text x="50" y="84.5" text-anchor="middle" fill="#8a8a8a" font-family="Barlow Condensed, sans-serif" font-size="5.5" letter-spacing="1.5">SERVICES LLC</text>
      <!-- Corner ornaments -->
      <path d="M22 16 L16 10 L22 10 Z" fill="#c0392b" opacity="0.5"/>
      <path d="M78 16 L84 10 L78 10 Z" fill="#c0392b" opacity="0.5"/>
    </svg>
    <div class="logo-text-group">
      <span class="logo-name">Ember Alert</span>
      <span class="logo-sub">Concrete Services · Est. Wake County</span>
    </div>
  </a>

  <ul class="nav-links">
    <li><a href="#services">Services</a></li>
    <li><a href="#process">Process</a></li>
    <li><a href="#work">Our Work</a></li>
    <li><a href="#testimonials">Reviews</a></li>
  </ul>

  <button class="nav-cta" onclick="document.getElementById('quote').scrollIntoView({behavior:'smooth'})">Free Quote</button>
</nav>

<!-- HERO -->
<section class="hero" id="home">
  <div class="hero-bg"></div>
  <div class="particles" id="particles"></div>
  <div class="hero-bg-number">01</div>

  <div class="hero-content">
    <div class="hero-eyebrow reveal">Wake County & Surrounding Areas, NC</div>
    <h1 class="hero-title reveal reveal-delay-1">
      <span class="line-dim">North Carolina's</span><br/>
      <span>Trusted</span><br/>
      <span class="line-accent">Concrete</span><br/>
      <span>Experts</span>
    </h1>
    <p class="hero-subtitle reveal reveal-delay-2">
      Built to last. Poured with pride. From driveways to foundations, we deliver precision concrete work that stands the test of time — and the Carolina weather.
    </p>
    <div class="hero-actions reveal reveal-delay-3">
      <a href="#quote" class="btn-primary">Get a Free Quote</a>
      <a href="#services" class="btn-ghost">View Services</a>
    </div>
  </div>

  <div class="hero-stats reveal reveal-delay-4">
    <div>
      <div class="hero-stat-num"><span id="stat1">0</span>+</div>
      <div class="hero-stat-label">Projects Completed</div>
    </div>
    <div>
      <div class="hero-stat-num"><span id="stat2">0</span>yr</div>
      <div class="hero-stat-label">Years in Business</div>
    </div>
    <div>
      <div class="hero-stat-num"><span id="stat3">0</span>%</div>
      <div class="hero-stat-label">Client Satisfaction</div>
    </div>
  </div>

  <div class="scroll-hint">
    <span>Scroll</span>
    <div class="scroll-line"></div>
  </div>
</section>

<!-- SERVICES -->
<section class="services" id="services">
  <div class="section-label reveal">What We Do</div>
  <h2 class="section-title reveal reveal-delay-1">Concrete Solutions<br/>Built for Every Need</h2>

  <div class="services-grid">
    <div class="service-card reveal">
      <span class="service-num">01</span>
      <svg class="service-icon" viewBox="0 0 48 48" fill="none" stroke="currentColor" stroke-width="1.5">
        <rect x="6" y="28" width="36" height="8" rx="1"/>
        <path d="M6 28 L10 14 L38 14 L42 28"/>
        <rect x="16" y="34" width="6" height="8"/>
        <rect x="26" y="34" width="6" height="8"/>
        <path d="M14 14 L16 6 L32 6 L34 14"/>
      </svg>
      <div class="service-name">Driveways</div>
      <div class="service-desc">Precision-poured residential and commercial driveways engineered for longevity and curb appeal. Stamped, brushed, or exposed aggregate finishes available.</div>
    </div>
    <div class="service-card reveal reveal-delay-1">
      <span class="service-num">02</span>
      <svg class="service-icon" viewBox="0 0 48 48" fill="none" stroke="currentColor" stroke-width="1.5">
        <rect x="6" y="30" width="36" height="12" rx="1"/>
        <rect x="10" y="18" width="28" height="12" rx="1"/>
        <rect x="14" y="8" width="20" height="10" rx="1"/>
        <line x1="14" y1="30" x2="14" y2="42"/>
        <line x1="34" y1="30" x2="34" y2="42"/>
      </svg>
      <div class="service-name">Patios</div>
      <div class="service-desc">Transform your outdoor living space with a beautifully finished concrete patio. Custom shapes, textures, and integrated drainage systems.</div>
    </div>
    <div class="service-card reveal reveal-delay-2">
      <span class="service-num">03</span>
      <svg class="service-icon" viewBox="0 0 48 48" fill="none" stroke="currentColor" stroke-width="1.5">
        <rect x="8" y="8" width="32" height="32" rx="1"/>
        <rect x="14" y="14" width="20" height="20" rx="1"/>
        <line x1="8" y1="20" x2="14" y2="20"/>
        <line x1="34" y1="20" x2="40" y2="20"/>
        <line x1="8" y1="28" x2="14" y2="28"/>
        <line x1="34" y1="28" x2="40" y2="28"/>
        <line x1="20" y1="8" x2="20" y2="14"/>
        <line x1="28" y1="8" x2="28" y2="14"/>
        <line x1="20" y1="34" x2="20" y2="40"/>
        <line x1="28" y1="34" x2="28" y2="40"/>
      </svg>
      <div class="service-name">Foundation Setting</div>
      <div class="service-desc">Structural foundation work done right the first time. We handle site prep, forming, reinforcement, and precision pouring for residential builds.</div>
    </div>
    <div class="service-card reveal reveal-delay-3">
      <span class="service-num">04</span>
      <svg class="service-icon" viewBox="0 0 48 48" fill="none" stroke="currentColor" stroke-width="1.5">
        <path d="M6 38 L14 30 L22 34 L30 24 L38 28 L42 22"/>
        <rect x="6" y="38" width="36" height="4" rx="1"/>
        <circle cx="14" cy="30" r="2" fill="currentColor"/>
        <circle cx="22" cy="34" r="2" fill="currentColor"/>
        <circle cx="30" cy="24" r="2" fill="currentColor"/>
        <circle cx="38" cy="28" r="2" fill="currentColor"/>
      </svg>
      <div class="service-name">Walkways</div>
      <div class="service-desc">Seamlessly connecting your property with clean, durable walkways. Custom widths, patterns, and edge detailing to match your landscape vision.</div>
    </div>
  </div>

  <div class="services-grid" style="margin-top:1px">
    <div class="service-card reveal">
      <span class="service-num">05</span>
      <svg class="service-icon" viewBox="0 0 48 48" fill="none" stroke="currentColor" stroke-width="1.5">
        <rect x="6" y="22" width="36" height="18" rx="1"/>
        <path d="M6 22 L24 8 L42 22"/>
        <rect x="18" y="30" width="12" height="10"/>
      </svg>
      <div class="service-name">Garage Floors</div>
      <div class="service-desc">Level, sealed, and reinforced garage floor pours with optional epoxy-ready finishes. Built to handle heavy vehicle loads for decades.</div>
    </div>
    <div class="service-card reveal reveal-delay-1">
      <span class="service-num">06</span>
      <svg class="service-icon" viewBox="0 0 48 48" fill="none" stroke="currentColor" stroke-width="1.5">
        <path d="M10 38 L10 18 Q10 10 18 10 L30 10 Q38 10 38 18 L38 38"/>
        <rect x="6" y="36" width="36" height="4" rx="1"/>
        <line x1="18" y1="10" x2="18" y2="36"/>
        <line x1="30" y1="10" x2="30" y2="36"/>
        <line x1="10" y1="22" x2="38" y2="22"/>
        <line x1="10" y1="30" x2="38" y2="30"/>
      </svg>
      <div class="service-name">Retaining Walls</div>
      <div class="service-desc">Engineered concrete retaining walls that manage grade changes and erosion control across your property — functional and sharp-looking.</div>
    </div>
    <div class="service-card reveal reveal-delay-2">
      <span class="service-num">07</span>
      <svg class="service-icon" viewBox="0 0 48 48" fill="none" stroke="currentColor" stroke-width="1.5">
        <rect x="8" y="32" width="32" height="8" rx="1"/>
        <path d="M8 32 L16 16 L32 16 L40 32"/>
        <line x1="20" y1="16" x2="20" y2="32"/>
        <line x1="28" y1="16" x2="28" y2="32"/>
        <path d="M16 16 L18 8 L30 8 L32 16"/>
      </svg>
      <div class="service-name">Steps & Stoops</div>
      <div class="service-desc">Entry steps and stoops that make a statement. Tight tolerances, clean risers, and non-slip finishes that hold up through North Carolina winters.</div>
    </div>
    <div class="service-card reveal reveal-delay-3">
      <span class="service-num">08</span>
      <svg class="service-icon" viewBox="0 0 48 48" fill="none" stroke="currentColor" stroke-width="1.5">
        <circle cx="24" cy="24" r="16"/>
        <circle cx="24" cy="24" r="8"/>
        <line x1="24" y1="8" x2="24" y2="16"/>
        <line x1="24" y1="32" x2="24" y2="40"/>
        <line x1="8" y1="24" x2="16" y2="24"/>
        <line x1="32" y1="24" x2="40" y2="24"/>
      </svg>
      <div class="service-name">Concrete Repair</div>
      <div class="service-desc">Crack injection, resurfacing, and section replacement for aging concrete. We restore instead of replace when possible — saving you money.</div>
    </div>
  </div>
</section>

<!-- STATS -->
<section class="stats-section" id="stats">
  <div class="stats-grid">
    <div class="stat-item">
      <span class="stat-number" data-target="500">0</span>
      <span class="stat-label">Projects Completed</span>
    </div>
    <div class="stat-item">
      <span class="stat-number" data-target="12">0</span>
      <span class="stat-label">Years of Experience</span>
    </div>
    <div class="stat-item">
      <span class="stat-number" data-target="98">0</span>
      <span class="stat-label">% Client Satisfaction</span>
    </div>
    <div class="stat-item">
      <span class="stat-number" data-target="15">0</span>
      <span class="stat-label">Counties Served</span>
    </div>
  </div>
</section>

<!-- BEFORE / AFTER -->
<section class="before-after-section" id="work">
  <div class="ba-header">
    <div>
      <div class="section-label reveal">Our Work</div>
      <h2 class="section-title reveal reveal-delay-1">See the<br/><span style="color:var(--ember)">Difference</span></h2>
    </div>
    <p class="ba-intro reveal reveal-delay-2">Drag the slider to compare before and after. Every project we take on gets the same obsessive attention to detail — whether it's a residential driveway or a commercial foundation.</p>
  </div>

  <div class="ba-slider-wrap reveal" id="ba-wrap">
    <!-- BEFORE side -->
    <div class="ba-before">
      <div class="ba-before-content" id="ba-before-content">
        <!-- Cracked old concrete look via CSS + SVG cracks -->
        <svg style="position:absolute;inset:0;width:100%;height:100%" viewBox="0 0 900 480" preserveAspectRatio="none">
          <rect width="900" height="480" fill="#2a2520"/>
          <!-- Large grid of aged sections -->
          <rect width="900" height="480" fill="url(#conc-old)"/>
          <!-- Crack lines -->
          <g stroke="#0a0806" stroke-width="2" opacity="0.8">
            <path d="M120 0 L140 60 L110 120 L150 200 L130 280 L160 350 L140 480" fill="none"/>
            <path d="M350 50 L330 120 L370 200 L340 300 L380 400 L360 480" fill="none"/>
            <path d="M600 0 L580 80 L620 160 L590 250 L630 340 L610 480" fill="none"/>
            <path d="M0 150 L80 160 L200 140 L350 160 L500 145 L650 165 L800 150 L900 155" fill="none"/>
            <path d="M0 320 L100 310 L250 330 L400 315 L560 335 L700 320 L900 325" fill="none"/>
            <path d="M140 60 L250 80 L300 60" fill="none"/>
            <path d="M330 120 L420 110 L500 130" fill="none"/>
            <path d="M580 80 L680 90 L750 70" fill="none"/>
          </g>
          <!-- Stain patches -->
          <ellipse cx="200" cy="200" rx="60" ry="40" fill="#1a1510" opacity="0.5"/>
          <ellipse cx="650" cy="320" rx="80" ry="30" fill="#1a1510" opacity="0.4"/>
          <ellipse cx="450" cy="100" rx="50" ry="25" fill="#1a1510" opacity="0.3"/>
          <!-- Spalling -->
          <circle cx="300" cy="250" r="15" fill="#0a0806" opacity="0.6"/>
          <circle cx="700" cy="150" r="10" fill="#0a0806" opacity="0.5"/>
          <defs>
            <pattern id="conc-old" x="0" y="0" width="30" height="30" patternUnits="userSpaceOnUse">
              <rect width="30" height="30" fill="none"/>
              <line x1="0" y1="0" x2="30" y2="0" stroke="#1f1d1a" stroke-width="0.5"/>
              <line x1="0" y1="0" x2="0" y2="30" stroke="#1f1d1a" stroke-width="0.5"/>
            </pattern>
          </defs>
        </svg>
      </div>
      <span class="ba-before-label">Before</span>
    </div>

    <!-- AFTER side with clip -->
    <div class="ba-clip" id="ba-after-clip" style="width:50%">
      <div class="ba-after" style="width:900px;max-width:none">
        <div class="ba-after-content">
          <svg style="position:absolute;inset:0;width:100%;height:100%" viewBox="0 0 900 480" preserveAspectRatio="none">
            <rect width="900" height="480" fill="#1c1b19"/>
            <!-- Clean fresh concrete look -->
            <rect width="900" height="480" fill="url(#conc-new)"/>
            <!-- Expansion joints - clean lines -->
            <g stroke="#0d0c0b" stroke-width="2.5">
              <line x1="0" y1="160" x2="900" y2="160"/>
              <line x1="0" y1="320" x2="900" y2="320"/>
              <line x1="300" y1="0" x2="300" y2="480"/>
              <line x1="600" y1="0" x2="600" y2="480"/>
            </g>
            <!-- Sheen / light reflection -->
            <rect x="0" y="0" width="900" height="480" fill="url(#sheen-grad)" opacity="0.08"/>
            <!-- Surface aggregate texture dots -->
            <g fill="#252320" opacity="0.4">
              <circle cx="80" cy="80" r="3"/><circle cx="200" cy="50" r="2"/>
              <circle cx="450" cy="240" r="3"/><circle cx="700" cy="400" r="2"/>
              <circle cx="150" cy="350" r="2"/><circle cx="800" cy="120" r="3"/>
              <circle cx="550" cy="80" r="2"/><circle cx="350" cy="420" r="3"/>
            </g>
            <defs>
              <pattern id="conc-new" x="0" y="0" width="60" height="60" patternUnits="userSpaceOnUse">
                <rect width="60" height="60" fill="none"/>
                <line x1="0" y1="0" x2="60" y2="0" stroke="#1a1917" stroke-width="0.3" opacity="0.5"/>
                <line x1="0" y1="0" x2="0" y2="60" stroke="#1a1917" stroke-width="0.3" opacity="0.5"/>
              </pattern>
              <linearGradient id="sheen-grad" x1="0" y1="0" x2="1" y2="1">
                <stop offset="0%" stop-color="white" stop-opacity="1"/>
                <stop offset="50%" stop-color="white" stop-opacity="0"/>
                <stop offset="100%" stop-color="white" stop-opacity="0.3"/>
              </linearGradient>
            </defs>
          </svg>
        </div>
        <span class="ba-after-label">After</span>
      </div>
    </div>

    <!-- Divider -->
    <div class="ba-divider" id="ba-divider" style="left:50%">
      <div class="ba-handle">
        <svg viewBox="0 0 18 18" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
          <path d="M6 4L2 9l4 5M12 4l4 5-4 5"/>
        </svg>
      </div>
    </div>
  </div>
</section>

<!-- PROCESS -->
<section class="process-section" id="process">
  <div class="section-label reveal">How It Works</div>
  <h2 class="section-title reveal reveal-delay-1">Our Process —<br/>No Surprises</h2>

  <div class="process-steps">
    <div class="process-step reveal">
      <div class="step-dot"><span class="step-dot-num">01</span></div>
      <div class="step-title">Free Consultation</div>
      <div class="step-desc">We come to your property, assess the site, and discuss your vision. No cost, no pressure.</div>
    </div>
    <div class="process-step reveal reveal-delay-1">
      <div class="step-dot"><span class="step-dot-num">02</span></div>
      <div class="step-title">Detailed Quote</div>
      <div class="step-desc">Receive a line-item quote within 24 hours. Transparent pricing, no hidden fees.</div>
    </div>
    <div class="process-step reveal reveal-delay-2">
      <div class="step-dot"><span class="step-dot-num">03</span></div>
      <div class="step-title">Site Prep</div>
      <div class="step-desc">We handle excavation, grading, forming, and rebar placement to code.</div>
    </div>
    <div class="process-step reveal reveal-delay-3">
      <div class="step-dot"><span class="step-dot-num">04</span></div>
      <div class="step-title">The Pour</div>
      <div class="step-desc">Precision poured and finished by our experienced crew using professional-grade equipment.</div>
    </div>
    <div class="process-step reveal reveal-delay-4">
      <div class="step-dot"><span class="step-dot-num">05</span></div>
      <div class="step-title">Final Walkthrough</div>
      <div class="step-desc">We walk the job with you at completion. You approve, we're done. Clean site guaranteed.</div>
    </div>
  </div>
</section>

<!-- TESTIMONIALS -->
<section class="testimonials-section" id="testimonials">
  <div class="section-label reveal">Client Reviews</div>
  <h2 class="section-title reveal reveal-delay-1">What Wake County<br/>Is Saying</h2>

  <div class="testimonials-track-wrap reveal reveal-delay-2">
    <div class="testimonials-track" id="testimonials-track">
      <!-- Cards duplicated for seamless loop -->
    </div>
  </div>
</section>

<!-- QUOTE -->
<section class="quote-section" id="quote">
  <div class="quote-inner">
    <div class="quote-left">
      <div class="section-label reveal">Get Started</div>
      <h2 class="section-title reveal reveal-delay-1">Ready to Build<br/>Something <span style="color:var(--ember)">Permanent?</span></h2>
      <p class="quote-copy reveal reveal-delay-2">Fill out the form and we'll get back to you within 24 hours with a no-obligation quote. We serve Wake County, Johnston County, Harnett County, and surrounding areas.</p>
      <div class="quote-trust reveal reveal-delay-3">
        <div class="trust-item"><span class="trust-dot"></span>Licensed & Insured</div>
        <div class="trust-item"><span class="trust-dot"></span>Free Estimates</div>
        <div class="trust-item"><span class="trust-dot"></span>5-Year Warranty</div>
      </div>
    </div>

    <form class="quote-form reveal reveal-delay-2" id="quote-form" onsubmit="handleSubmit(event)">
      <div class="form-row">
        <div class="form-group">
          <label class="form-label">First Name</label>
          <input class="form-input" type="text" placeholder="John"/>
        </div>
        <div class="form-group">
          <label class="form-label">Last Name</label>
          <input class="form-input" type="text" placeholder="Smith"/>
        </div>
      </div>
      <div class="form-row">
        <div class="form-group">
          <label class="form-label">Phone</label>
          <input class="form-input" type="tel" placeholder="(919) 000-0000"/>
        </div>
        <div class="form-group">
          <label class="form-label">Email</label>
          <input class="form-input" type="email" placeholder="john@email.com"/>
        </div>
      </div>
      <div class="form-group">
        <label class="form-label">Service Needed</label>
        <select class="form-select">
          <option value="">Select a service...</option>
          <option>Driveway</option>
          <option>Patio</option>
          <option>Foundation</option>
          <option>Walkway</option>
          <option>Garage Floor</option>
          <option>Retaining Wall</option>
          <option>Steps & Stoop</option>
          <option>Concrete Repair</option>
          <option>Other / Multiple</option>
        </select>
      </div>
      <div class="form-group">
        <label class="form-label">Project Details</label>
        <textarea class="form-textarea" placeholder="Tell us about your project — size, timeline, any special requirements..."></textarea>
      </div>
      <button type="submit" class="form-submit" id="submit-btn">Request Free Quote</button>
    </form>
  </div>
</section>

<!-- FOOTER -->
<footer>
  <div>
    <div class="footer-copy">© 2024 Ember Alert Services LLC · Wake County, NC · All Rights Reserved</div>
    <div class="footer-copy" style="margin-top:0.3rem">Licensed · Insured · Locally Owned & Operated</div>
  </div>
  <div class="footer-tagline">Built to Last. Poured with Pride.</div>
</footer>

<script>
// CUSTOM CURSOR
const cursor = document.getElementById('cursor');
const cursorRing = document.getElementById('cursor-ring');
let mx = 0, my = 0, rx = 0, ry = 0;
document.addEventListener('mousemove', e => { mx = e.clientX; my = e.clientY; });
function animateCursor() {
  rx += (mx - rx) * 0.12;
  ry += (my - ry) * 0.12;
  cursor.style.left = mx + 'px';
  cursor.style.top = my + 'px';
  cursorRing.style.left = rx + 'px';
  cursorRing.style.top = ry + 'px';
  requestAnimationFrame(animateCursor);
}
animateCursor();

// PARTICLES
const particlesEl = document.getElementById('particles');
for (let i = 0; i < 28; i++) {
  const p = document.createElement('div');
  p.className = 'particle';
  p.style.cssText = `
    left: ${Math.random() * 100}%;
    bottom: ${Math.random() * 20}%;
    animation-duration: ${3 + Math.random() * 5}s;
    animation-delay: ${Math.random() * 6}s;
    --drift: ${(Math.random() - 0.5) * 60}px;
    width: ${2 + Math.random() * 3}px;
    height: ${2 + Math.random() * 3}px;
    opacity: ${0.7 + Math.random() * 0.3};
  `;
  particlesEl.appendChild(p);
}

// NAV SCROLL
const nav = document.getElementById('main-nav');
window.addEventListener('scroll', () => {
  nav.classList.toggle('scrolled', window.scrollY > 60);
});

// HERO STAT COUNTERS
function countUp(el, target, duration) {
  let start = 0;
  const step = target / (duration / 16);
  const timer = setInterval(() => {
    start += step;
    if (start >= target) { el.textContent = target; clearInterval(timer); return; }
    el.textContent = Math.floor(start);
  }, 16);
}

// REVEAL ON SCROLL
const reveals = document.querySelectorAll('.reveal');
const observer = new IntersectionObserver((entries) => {
  entries.forEach(e => {
    if (e.isIntersecting) {
      e.target.classList.add('visible');
      observer.unobserve(e.target);
    }
  });
}, { threshold: 0.1 });
reveals.forEach(r => observer.observe(r));

// Counter observer
let counted = false;
const statsSection = document.querySelector('.stats-section');
const counterObserver = new IntersectionObserver((entries) => {
  if (entries[0].isIntersecting && !counted) {
    counted = true;
    document.querySelectorAll('.stat-number[data-target]').forEach(el => {
      countUp(el, parseInt(el.dataset.target), 1800);
    });
    countUp(document.getElementById('stat1'), 500, 1800);
    countUp(document.getElementById('stat2'), 12, 1800);
    countUp(document.getElementById('stat3'), 98, 1800);
  }
}, { threshold: 0.3 });
counterObserver.observe(statsSection);

// BEFORE/AFTER SLIDER
const baWrap = document.getElementById('ba-wrap');
const baDivider = document.getElementById('ba-divider');
const baClip = document.getElementById('ba-after-clip');
let isDragging = false;

function setSlider(pct) {
  pct = Math.max(5, Math.min(95, pct));
  baDivider.style.left = pct + '%';
  baClip.style.width = pct + '%';
}

baWrap.addEventListener('mousedown', () => isDragging = true);
window.addEventListener('mouseup', () => isDragging = false);
baWrap.addEventListener('mousemove', e => {
  if (!isDragging && e.type !== 'touchmove') return;
  const rect = baWrap.getBoundingClientRect();
  const x = (e.clientX || e.touches[0].clientX) - rect.left;
  setSlider((x / rect.width) * 100);
});
baWrap.addEventListener('touchstart', () => isDragging = true);
baWrap.addEventListener('touchend', () => isDragging = false);
baWrap.addEventListener('touchmove', e => {
  e.preventDefault();
  const rect = baWrap.getBoundingClientRect();
  setSlider(((e.touches[0].clientX - rect.left) / rect.width) * 100);
});

// Animate slider in on scroll
const baObserver = new IntersectionObserver(entries => {
  if (entries[0].isIntersecting) {
    let pos = 50, dir = -1;
    const intro = setInterval(() => {
      pos += dir * 0.5;
      if (pos <= 30) dir = 1;
      if (pos >= 50) { clearInterval(intro); setSlider(50); }
      setSlider(pos);
    }, 16);
    baObserver.disconnect();
  }
}, { threshold: 0.5 });
baObserver.observe(baWrap);

// TESTIMONIALS
const testimonials = [
  { name: 'Marcus T.', location: 'Raleigh, NC', service: 'Driveway', text: 'Ember Alert completely transformed our property. The driveway they poured is flawless — even lines, perfect slope, and they finished ahead of schedule. Neighbors keep asking who did it.', initial: 'M' },
  { name: 'Sandra K.', location: 'Cary, NC', service: 'Patio', text: 'From the first call to the final walkthrough, these guys were professional every step. The patio looks like it belongs in a magazine. Concrete work at this level is rare.', initial: 'S' },
  { name: 'David R.', location: 'Apex, NC', service: 'Foundation', text: 'We needed a slab foundation for a workshop addition. The crew showed up on time, communicated clearly, and the pour was executed perfectly. No cracks, level as a table.', initial: 'D' },
  { name: 'Tanya M.', location: 'Garner, NC', service: 'Walkways', text: 'The walkway and front stoop they built make the whole front yard look upscale. Clean lines, great finish. Worth every penny and then some.', initial: 'T' },
  { name: 'James P.', location: 'Wake Forest, NC', service: 'Repair', text: 'Had severe cracking in my garage floor from years of settling. Ember Alert came out, diagnosed the issue, and repaired it for a fraction of what others quoted for full replacement.', initial: 'J' },
  { name: 'Rachel H.', location: 'Holly Springs, NC', service: 'Patio', text: "Best contractor experience I've had in 20 years of homeownership. They respected my property, cleaned up every day, and the finished patio is absolutely stunning.", initial: 'R' },
];

const track = document.getElementById('testimonials-track');
const allCards = [...testimonials, ...testimonials];
allCards.forEach(t => {
  const card = document.createElement('div');
  card.className = 'testimonial-card';
  card.innerHTML = `
    <span class="testimonial-service-tag">${t.service}</span>
    <div class="testimonial-stars">${'<span class="star">★</span>'.repeat(5)}</div>
    <p class="testimonial-text">"${t.text}"</p>
    <div class="testimonial-author">
      <div class="author-avatar">${t.initial}</div>
      <div>
        <div class="author-name">${t.name}</div>
        <div class="author-location">${t.location}</div>
      </div>
    </div>
  `;
  track.appendChild(card);
});

// FORM SUBMIT
function handleSubmit(e) {
  e.preventDefault();
  const btn = document.getElementById('submit-btn');
  btn.textContent = 'Sending...';
  btn.disabled = true;
  setTimeout(() => {
    btn.textContent = 'Quote Request Sent!';
    btn.classList.add('submitted');
    setTimeout(() => {
      btn.textContent = 'Request Free Quote';
      btn.classList.remove('submitted');
      btn.disabled = false;
      e.target.reset();
    }, 3000);
  }, 1200);
}
</script>
</body>
</html>
